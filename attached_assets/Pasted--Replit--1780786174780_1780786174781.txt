کاملاً حق داری. پرامپت قبلی برای **کل محصول** بود و ممکنه Replit گیج بشه یا پروژه رو سنگین کنه.

الان فاز اول فقط باید این باشه:

# هدف فاز اول

ساخت یک وب‌اپ شیک و کاربردی که فقط این ۳ چیز را درست نشان دهد:

1. **Owner Profile**
2. **Sponsor Teaser**
3. **Lead Form + CRM Preview**

نه Marketplace.
نه Portal کامل.
نه سیستم بزرگ.

این پرامپت فاز اول را بده:

```text
فعلاً فقط Phase 1 را انجام بده.

کد جدید زیاد نساز. پروژه را سنگین نکن.  
هدف من الان ساخت نسخه MVP ساده، شیک و قابل نمایش است.

محصول VELOXA یک NFC Business Card ساده نیست، اما در فاز اول فقط باید این جریان را بسازیم:

NFC Tap
↓
Sponsor Teaser
↓
Lead Form
↓
Owner Profile
↓
Admin Lead List

====================================================

وظیفه تو در Phase 1

فقط این صفحات را تمیز و مدرن کن:

1. Public Owner Profile
2. Sponsor Teaser
3. Lead Capture Form
4. Admin Sponsor Leads Page

====================================================

فعلاً نساز:

- Marketplace
- Sponsor Portal
- Owner Portal کامل
- Referral System
- Payment System
- AI Features
- Complex Dashboard

====================================================

طراحی باید:

- مدرن
- ساده
- موبایل‌فرست
- لوکس
- Dark / Premium
- قابل ارائه به مشتری

باشد.

====================================================

اولویت‌ها:

1. وقتی کارت NFC باز می‌شود، اول Sponsor Teaser شیک نمایش داده شود.
2. روی Teaser دکمه CTA باشد.
3. با CTA فرم Lead باز شود.
4. Lead در CRM/Admin ذخیره شود.
5. بعد Owner Profile نمایش داده شود.
6. در Admin بتوان Leadها را دید و Status آنها را عوض کرد.

====================================================

قبل از تغییرات:

اول فایل‌های فعلی مربوط به این بخش‌ها را پیدا کن:

- PublicProfilePage
- SponsorTeaser
- SponsorLeadModal
- AdminSponsorsPage
- SponsorLeadsPage
- sponsors API
- sponsor_leads table

فقط همین بخش‌ها را بررسی کن.

====================================================

بعد از بررسی:

به من بگو:

1. کدام فایل‌ها اصلی هستند؟
2. کدام فایل‌ها را باید تغییر بدهی؟
3. دقیقاً چه تغییراتی لازم است؟
4. آیا چیزی اضافه لازم است یا نه؟

تا وقتی من تأیید نکردم، کدنویسی نکن.

====================================================

قانون مهم:

VELOXA را به عنوان NFC Card App توسعه نده.

اما در Phase 1 فقط روی همین MVP تمرکز کن:

Sponsor Teaser + Lead Capture + Owner Profile + Admin Leads

همین.
```

این خیلی بهتره.
هم Vision را نگه می‌داره، هم Replit را خفه نمی‌کند.
